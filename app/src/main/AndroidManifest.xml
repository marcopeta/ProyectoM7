<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="es.gracia.marcos.proyectom7">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".ModificarConsejoActivity"></activity>
        <activity android:name=".ui.consejos.AnadirConsejoActivity" />
        <activity android:name=".ui.inicio.AnadirAlimentoDiaActivity" />
        <activity android:name=".ModificarAlimentoActivity" />
        <activity android:name=".RecuperarContrasenaActivity" />
        <activity
            android:name=".CajaNavegacionActivity"
            android:label="@string/title_activity_inicio"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".RegistroActivity" />
        <activity android:name=".AnadirAlimentoActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>